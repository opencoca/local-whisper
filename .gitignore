# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
*.xccheckout
*.moved-aside

# macOS
.DS_Store
*.swp
*~.nib

# Stray screenshots dragged into the repo root by mistake. Real screenshots
# for docs live under docs/images/. The `/` anchor means we only ignore
# files matching at the repo root, not anywhere under it.
/Screenshot*.png
/Screenshot*.jpg

# Build products
build/
*.app
*.dSYM
dist/

# IDE
.idea/
*.code-workspace
.vscode/

# iOS bundled Whisper model files — downloaded by `make ios_models` from
# HuggingFace argmaxinc/whisperkit-coreml. Per-contributor, not in git.
# The folder itself (with .gitkeep) is tracked; the .mlmodelc subfolders aren't.
Talking/Mobile/Resources/Models/openai_whisper-*/

# AppIcon build artifacts — regenerated by build-icon.sh.
# The SVG + source PNGs + final .icns are tracked; the intermediate
# iconset directory and the 1024-master PNG are not.
Talking/Resources/AppIcon-source/AppIcon.iconset/
Talking/Resources/AppIcon-source/AppIcon-1024.png
